# all-temp-fork-pub-scheduled_sync_with_upstream-weekly.yaml
# This is a template and should not be changed unless you are familiar with the reusable GitHub Actions strategy of the company.
name: all-temp-fork-pub-scheduled_sync_with_upstream-weekly

on:
  schedule:
    - cron: '0 0 * * 0'  # Runs weekly on Sunday at midnight UTC
  workflow_dispatch:      # Allows manual trigger

jobs:
  all-temp-fork-pub-scheduled_sync_with_upstream-weekly:
    permissions:
      id-token: write
      contents: write
    uses: BrightEdgeeServices/devops-automation/.github/workflows/all-wf-fork-pub-scheduled_sync_with_upstream-def.yaml@master
    secrets:
      # GH_REPO_ACCESS_BEE_MASTER: ${{secrets.GH_REPO_ACCESS_BEE_MASTER }}
      # GH_REPO_ACCESS_RTE_MASTER: ${{secrets.GH_REPO_ACCESS_RTE_MASTER }}
      # MYSQL_PASSWORD:            ${{secrets.MYSQL_PASSWORD}}
      # MYSQL_ROOT_PASSWORD:       ${{secrets.MYSQL_ROOT_PASSWORD}}
      # MYSQL_USER:                ${{secrets.MYSQL_USER}}
      # PYPI_API_TOKEN_PROD:       ${{secrets.PYPI_API_TOKEN_PROD}}
      ORIGINAL_OWNER:       ${{vars.ORIGINAL_OWNER}}
      ORIGINAL_REPO_NAME:   ${{vars.ORIGINAL_REPO_NAME}}
      # RELEASE_EMAIL_USER:        ${{secrets.RELEASE_EMAIL_USER }}
      # RELEASE_EMAIL_PASSWORD:    ${{secrets.RELEASE_EMAIL_PASSWORD }}
