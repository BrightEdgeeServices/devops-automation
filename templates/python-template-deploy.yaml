# GitHub Actions Script: python-template-deploy.yaml
# This is a template and should not be changed unless you are familiar with the
# reusable GitHub Actions strategy of the company.
name: Python Default Template

on:
  push:

jobs:
  default-deployment-pipeline:
    if: github.event.pull_request.merged == true
    uses: RealTimeEvents/devops-automation/.github/workflows/python-wf-default-deploy-pipeline.yaml@master
    with:
      run_deploy: ${{ github.event.pull_request.base.ref == 'main'}}
    secrets: inherit
